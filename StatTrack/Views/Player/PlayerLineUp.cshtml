@using StatTrack.Controllers
@model IEnumerable<StatTrack.Models.TeamPlayerModel>

@{
    ViewData["Title"] = "PlayerLineUp";
    var id = PlayerController.currentTeamId;


}

<h1>Spillerlist for spillere på hold </h1>

<!-- Tilføj spiller knap, skal åbne en modal -->

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.YOB)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Position)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.YOB)
                </td>
                <td>
                    @Html.ActionLink("Rediger", "Edit", new { playerId = item.PlayerID, playerName = item.Name, playerPosition = item.Position, YOB = item.YOB }, new { @class = "btn btn-outline-warning" })
                    @Html.ActionLink("Info", "Details", new { playerId = item.PlayerID, playerName = item.Name, playerPosition = item.Position, YOB = item.YOB }, new { @class = "btn btn-outline-primary" })
                    @Html.ActionLink("Slet", "Delete", new { playerId = item.PlayerID, playerName = item.Name, playerPosition = item.Position, YOB = item.YOB }, new { @class = "btn btn-outline-danger" })
                </td>
            </tr>
        }
    </tbody>
</table>
